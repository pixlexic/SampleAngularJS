import { SampleItemModel } from '../Models/sample-item-model';

describe('SampleItemModel', () => {
  it('should create an instance', () => {
    expect(new SampleItemModel()).toBeTruthy();
  });
});
